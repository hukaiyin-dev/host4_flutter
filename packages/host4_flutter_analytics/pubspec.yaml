name: host4_flutter_analytics
description: "Minimal event model and multi-sink analytics helpers for host4_flutter."
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  collection: ^1.19.1
  host4_flutter_core:
    path: ../host4_flutter_core

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
