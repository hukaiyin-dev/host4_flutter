name: host4_flutter_core
description: "Shared abstractions and protocols for the host4_flutter SDK."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.10.0 <4.0.0"

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
