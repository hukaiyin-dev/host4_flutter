name: host4_flutter_aivoice
description: "Flutter plugin for VolcEngine RTC AI Voice."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: Host4FlutterAiVoicePlugin
