name: host4_flutter_crypto
description: "Minimal hashing, HMAC, and base64 helpers for host4_flutter."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  crypto: ^3.0.6

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
