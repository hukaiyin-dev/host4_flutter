name: host4_flutter_uart
description: "UART transport facade that binds Host4 to the in-app DeviceBroker."
version: 0.0.1
publish_to: none
homepage:

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  host4_flutter_device_native:
    path: ../host4_flutter_device_native
  host4_flutter_transport:
    path: ../host4_flutter_transport

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
